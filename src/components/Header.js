import React from 'react';

const Header = () => {
  return (
    <header className={`text-dark mt-70`}>
      <div className="title-header">✔️ Task List</div>
      <div className="desc-header">
        Break your life to simple tasks to get things done! Does not matter how many tasks you done, It’s important to
        break to small tasks and be on progress.
      </div>
    </header>
  );
};

export default Header;
